# Contributing

When submitting a new TikTok Web build snapshot:

1. Record the capture date/build context.
2. Keep generated schema/route data separate from handwritten documentation.
3. Do not include cookies, tokens, private keys, or account identifiers.
4. Call out changed command numbers, protobuf fields, and routes explicitly.
5. Prefer reproducible extraction scripts over manual transcription.
